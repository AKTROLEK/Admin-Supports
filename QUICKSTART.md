# دليل البداية السريعة ⚡

## التشغيل في 5 دقائق

### 1. التحضير (دقيقة واحدة)

```bash
# استنساخ المشروع
git clone https://github.com/AKTROLEK/Admin-Supports.git
cd Admin-Supports

# تثبيت المتطلبات
npm install
```

### 2. إنشاء البوت في Discord (دقيقتان)

1. اذهب إلى https://discord.com/developers/applications
2. اضغط **New Application**
3. أدخل اسم البوت → **Create**
4. في قسم **Bot**:
   - اضغط **Add Bot**
   - فعّل جميع **Privileged Gateway Intents**
   - انسخ الـ **Token**
5. في قسم **OAuth2 → URL Generator**:
   - Scopes: ✅ bot ✅ applications.commands
   - Bot Permissions: ✅ Administrator (للتبسيط)
   - انسخ الرابط وافتحه لإضافة البوت

### 3. التكوين (دقيقة واحدة)

```bash
# إنشاء ملف البيئة
cp .env.example .env
```

**عدل ملف `.env`:**
```env
DISCORD_TOKEN=YOUR_BOT_TOKEN_HERE
CLIENT_ID=YOUR_APPLICATION_ID_HERE
GUILD_ID=YOUR_SERVER_ID_HERE

DEFAULT_LANGUAGE=ar
ADMIN_ROLES=your_admin_role_id
MANAGEMENT_ROLES=your_management_role_id
```

**للحصول على المعرفات:**
- فعّل وضع المطور في Discord: إعدادات → متقدم → وضع المطور
- انقر بزر الماوس الأيمن على السيرفر/الرتبة → نسخ المعرف

### 4. التشغيل (10 ثواني)

```bash
npm start
```

**يجب أن تشاهد:**
```
✅ Database initialized successfully
✅ Loaded command: stats-daily
...
✅ Bot is online as YourBot#1234
```

### 5. التجربة (دقيقة واحدة)

في Discord:
1. اكتب `/` وشاهد أوامر البوت
2. جرب `/dashboard` لعرض لوحة التحكم
3. ادخل روم صوتي ثم اخرج
4. استخدم `/stats-daily` لعرض إحصائياتك

## الأوامر الأساسية

### 📊 الإحصائيات
```
/stats-daily         إحصائيات اليوم
/stats-weekly        إحصائيات الأسبوع
/stats-monthly       إحصائيات الشهر
/compare @user1 @user2   مقارنة إداريين
```

### 🎯 الإدارة
```
/dashboard           لوحة التحكم
/note add @user نص   إضافة ملاحظة
/note view @user     عرض ملاحظات
```

### 💾 النسخ الاحتياطي
```
/backup create       إنشاء نسخة
/backup list         عرض النسخ
/export              تصدير البيانات
```

### ⚙️ الإعدادات
```
/language ar|en      تغيير اللغة
```

## المشاكل الشائعة وحلولها

### ❌ البوت لا يعمل
```bash
# تأكد من صحة Token
echo $DISCORD_TOKEN

# تأكد من تثبيت المتطلبات
npm install
```

### ❌ الأوامر لا تظهر
1. تأكد من صحة `CLIENT_ID` و `GUILD_ID`
2. انتظر 5 دقائق
3. أعد تشغيل Discord
4. جرب `/` في السيرفر

### ❌ خطأ في الصلاحيات
- تأكد من منح البوت صلاحية Administrator
- تأكد من أن البوت فوق الرتب المستهدفة

### ❌ لا يتتبع الصوت
1. تأكد من تفعيل `PRESENCE INTENT` و `SERVER MEMBERS INTENT`
2. أعد تشغيل البوت

## نصائح سريعة

### 🚀 الأداء
```bash
# استخدم PM2 للتشغيل المستمر
npm install -g pm2
pm2 start src/index.js --name fivem-bot
pm2 save
```

### 📝 المراقبة
```bash
# عرض السجلات
pm2 logs fivem-bot

# عرض الحالة
pm2 status
```

### 🔒 الأمان
```bash
# لا تشارك ملف .env
# لا تنشر Token البوت
# استخدم النسخ الاحتياطي
/backup create
```

### 🎨 التخصيص
```javascript
// تغيير لون الـ embeds في src/utils/helpers.js
export const EmbedColors = {
    STATS: 0x9b59b6,  // بنفسجي
    SUCCESS: 0x00ff00, // أخضر
    ERROR: 0xff0000    // أحمر
}
```

## الخطوات التالية

1. ✅ **اقرأ** [SETUP.md](SETUP.md) للتفاصيل الكاملة
2. ✅ **راجع** [ARCHITECTURE.md](ARCHITECTURE.md) لفهم البنية
3. ✅ **جرب** جميع الأوامر
4. ✅ **خصص** حسب احتياجاتك
5. ✅ **شارك** ملاحظاتك

## الدعم

🐛 **مشكلة؟** افتح [Issue](https://github.com/AKTROLEK/Admin-Supports/issues)

💡 **اقتراح؟** ارسل Pull Request

📖 **سؤال؟** راجع الوثائق

---

**تهانينا! 🎉 البوت الآن يعمل بنجاح!**

استمتع بتتبع إحصائيات إداريي FiveM! 🚀
